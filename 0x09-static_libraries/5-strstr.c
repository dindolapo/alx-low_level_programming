#include "main.h"
#include <stdio.h>

/**
 * _atoi - converts a string to an integer
 * @s: input arg
 * Return: integer or 0 if there are no numbers in the string
 */
char *_strstr(char *haystack, char *needle)

{
	(void)*haystack;
	(void)*needle;

		return (0);
}
