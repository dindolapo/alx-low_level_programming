#include "main.h"

/**
 * _puts - prints a string to the stdout
 * @str: input string
 * Return: void
 */
void _puts(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (i % 2 != 0)
		{
		}
		else
		{
			_putchar(str[i]);
		}
	}
	_putchar('\n');
}
